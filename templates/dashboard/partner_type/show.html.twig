{% extends 'base.html.twig' %}

{% block title %}PartnerType{% endblock %}

{% block body %}
    <h1>PartnerType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partner_type_index') }}">back to list</a>

    <a href="{{ path('app_partner_type_edit', {'id': partner_type.id}) }}">edit</a>

    {{ include('partner_type/_delete_form.html.twig') }}
{% endblock %}
